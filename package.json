{
  "name": "chinese-quick-mapping",
  "version": "1.0.0",
  "repository": "git@github.com:chauchakching/chinese-quick-mapping.git",
  "author": "chauchakching <chauchakching@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "cp -r src/public/* dist/ && elm-live src/Main.elm --open --dir=dist --pushstate -- --output=dist/main.js",
    "format": "elm-format src/Main.elm --yes",
    "build": "elm make --output=dist/main.js src/Main.elm && cp -r src/public/* dist/",
    "gh-pages-publish": "git checkout gh-pages && git merge master --no-edit && yarn build && cp dist/* . && git add . && git commit -m 'publish demo' && git push && git checkout master"
  },
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
